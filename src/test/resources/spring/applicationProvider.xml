<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans.xsd
     http://code.alibabatech.com/schema/dubbo
     http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


    <!-- 提供方应用信息，用于计算依赖关系
       name 必填项
       version 版本
       owner 接口负责人
    -->
    <dubbo:application name="app-provider" owner="WAT.qq.com" version="1.0.0" organization="C.W.S" logger="log4j"/>

    <!-- 使用zookeeper注册中心暴露服务地址
       address 必填 注册中心地址
    -->
    <dubbo:registry protocol="dubbo" address="zookeeper://47.91.212.82:2181"/>

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880" accesslog="true"/>

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.wat.springmvc.web.service.DemoService" ref="demoService"/>

    <!-- 和本地bean一样实现服务 -->
    <bean id="demoService" class="com.wat.springmvc.web.service.impl.DemoServiceImpl"/>


</beans>